package MathQuest.Logic;
/*
import static org.junit.Assert.*;

import org.junit.After;
import org.junit.Test;

public class EquationTest {

	String equation;
	Integer answer;
	Integer falseAnswer;
	
	@After
	public void tearDown() {
		equation = null;
		answer = null;
		falseAnswer = null;
	}
	
	@Test
	public void testAddition() {
		equation = "5 + 2";
		answer = Equation.solveEquation(equation);
		falseAnswer = Equation.generateWrongAnswer(equation);
		
		assertNotEquals(answer, falseAnswer);
		assertEquals(7, (int)answer);
	}

	@Test
	public void testSubtraction() {
		equation = "5 - 2";
		answer = Equation.solveEquation(equation);
		falseAnswer = Equation.generateWrongAnswer(equation);
		
		assertNotEquals(answer, falseAnswer);
		assertEquals(3, (int)answer);
	}
	
	@Test
	public void testMultiAddition() {
		equation = "15 + 10 + 19";
		answer = Equation.solveEquation(equation);
		falseAnswer = Equation.generateWrongAnswer(equation);
		
		assertNotEquals(answer, falseAnswer);
		assertEquals(44, (int)answer);
	}
	
	@Test
	public void testMultipleSubtraction() {
		equation = "15 - 10 - 19";
		answer = Equation.solveEquation(equation);
		falseAnswer = Equation.generateWrongAnswer(equation);
		
		assertNotEquals(answer, falseAnswer);
		assertEquals(-14, (int)answer);
	}
}*/
